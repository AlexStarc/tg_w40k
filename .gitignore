# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.DS_Store
node_modules/
package-lock.json
package.json

# Virtual environments
.venv
venv/

# Secrets
.env

# Telethon sessions
*.session
*.session-journal

# Collected message data
data/
bot.db
bot_server.db

# Downloaded socks5 proxy pool (re-fetched at runtime if missing)
proxies_pool.txt
proxies_priority.txt

# Generated meme artifacts (never commit — reproducible at runtime)
memes_cache/
# Live quote bank — mutated at runtime; seeded from bank.seed.json on first run
bank.json

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
